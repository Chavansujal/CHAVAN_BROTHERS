Place your background music file here as:

audio/jazz-piano.mp3

The website is already configured to autoplay it on load, loop it continuously,
and retry playback on the visitor's first interaction if the browser blocks
autoplay with sound.
